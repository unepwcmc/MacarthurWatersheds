<div class="selector">
	<h1>{{title}}</h1>

	<select id="lens-select" class='select-box'>
	  {{# each lenses}}
	  <option value="{{selector}}" {{#if selected}}selected{{/if}}>{{name}}</option>
	  {{/each}}
	</select>
</div>
